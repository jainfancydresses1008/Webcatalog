import AdminShell from '@/components/admin/AdminShell';
import PermanentDeleteButton from '@/components/admin/PermanentDeleteButton';
import { requireAdminSession } from '@/lib/admin-auth';
import { prisma } from '@/lib/prisma';
import { permanentlyDeleteDress } from '../../actions';
export const dynamic='force-dynamic';
export default async function AdminCleanupPage(){await requireAdminSession();const deleted=await prisma.dress.findMany({where:{isActive:false,deletedAt:{not:null}},include:{images:true},orderBy:{deletedAt:'asc'}});return <AdminShell><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-2xl font-black text-slate-950">Database Cleanup</h2><p className="mt-1 text-sm text-slate-500">Soft-deleted dresses remain here until you explicitly and PIN-confirmed permanently delete them. Their Cloudinary images are retained for reuse.</p><div className="mt-5 space-y-3">{deleted.length===0?<div className="rounded-2xl bg-slate-50 p-5 text-sm text-slate-500">No soft-deleted dresses are waiting for cleanup.</div>:deleted.map(dress=><div key={dress.id} className="flex flex-col gap-4 rounded-2xl border border-red-100 bg-red-50/40 p-4 md:flex-row md:items-center md:justify-between"><div><p className="font-black text-slate-900">{dress.characterName} <span className="text-slate-500">#{dress.id}</span></p><p className="text-xs text-slate-500">Deleted {dress.deletedAt?.toLocaleString()} by {dress.deletedBy??'unknown'} · {dress.images.length} image records retained</p></div><PermanentDeleteButton description={`${dress.characterName} (#${dress.id}) and its database child records`} fieldName="dressId" fieldValue={dress.id} action={permanentlyDeleteDress}/></div>)}</div></section></AdminShell>}
