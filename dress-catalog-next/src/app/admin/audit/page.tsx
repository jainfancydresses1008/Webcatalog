import AdminShell from '@/components/admin/AdminShell';
import { requireAdminSession } from '@/lib/admin-auth';
import { prisma } from '@/lib/prisma';
export const dynamic='force-dynamic';
export default async function AdminAuditPage(){await requireAdminSession();const logs=await prisma.adminAuditLog.findMany({orderBy:{createdAt:'desc'},take:250});return <AdminShell><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-2xl font-black text-slate-950">Audit Logs</h2><p className="mt-1 text-sm text-slate-500">Recent administrative actions recorded by the application.</p><div className="mt-5 overflow-x-auto rounded-2xl border border-slate-200"><table className="w-full text-left text-sm"><thead className="bg-slate-100"><tr><th className="p-3">Time</th><th className="p-3">Admin</th><th className="p-3">Action</th><th className="p-3">Entity</th><th className="p-3">ID</th><th className="p-3">Details</th></tr></thead><tbody>{logs.map(log=><tr key={log.id} className="border-t border-slate-100 align-top"><td className="whitespace-nowrap p-3 text-slate-500">{log.createdAt.toLocaleString()}</td><td className="p-3 font-semibold text-slate-700">{log.adminEmail}</td><td className="p-3 font-black text-slate-900">{log.action}</td><td className="p-3">{log.entity}</td><td className="p-3">{log.entityId??'—'}</td><td className="max-w-xl whitespace-pre-wrap break-words p-3 text-xs text-slate-500">{log.details??'—'}</td></tr>)}</tbody></table></div></section></AdminShell>}
